#pragma once
#include <windows.h>

int main(); // need prot.

void InvisibleCursor(); // make the cursor invisible

void SetCursor(SHORT x, SHORT y);

void CenrtreConsole(); // setting the size of the application window

void ChangeColour(size_t c); // function for painting characters
